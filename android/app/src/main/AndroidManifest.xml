<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Needed to ask Android to exempt the app from battery optimisation so
         alarms fire on time even on aggressive OEM builds (Samsung, Xiaomi…) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- POST_NOTIFICATIONS is handled by the @capacitor/local-notifications plugin's
         own merged manifest — no manual declaration needed here. -->

    <!-- SCHEDULE_EXACT_ALARM: needed on Android 12/12L (API 31-32) for precise alarm
         delivery. maxSdkVersion="32" keeps it out of the manifest on API 33+ where the
         plugin handles scheduling differently. On Android 10/11 (API 29-30) the system
         ignores this unknown permission gracefully — it does not prevent installation.
         USE_EXACT_ALARM has been removed: it is restricted to system alarm-clock apps
         and caused installation failures on strict OEM builds running Android 10/11. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
</manifest>
